import { style } from '@vanilla-extract/css';

export const styles = {
  imgLogo: style({
    display: 'block',
    margin: 'auto',
  }),
  counterDiv: style({
    textAlign: 'center',
    lineHeight: '1.8',
  }),
};
